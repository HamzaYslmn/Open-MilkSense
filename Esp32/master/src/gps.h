#pragma once
// The master has no GPS of its own — it forwards the GPS reported by each slave.
// File kept (and intentionally empty) so the sketch layout matches the slave's.
// If you ever want a fixed base/geofence reference, mirror slave/src/gps.h here.
